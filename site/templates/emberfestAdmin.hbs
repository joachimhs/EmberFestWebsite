<div class="row">
    <div class="small-12 large-6 columns">
        <h2>Keys</h2>
        <ul class="side-nav adminList">
            {{#each data in controller}}
                {{#link-to "emberfestAdmin.key" data tagName="li"}}
                    {{data.id}}
                {{/link-to}}
            {{/each}}
        </ul>
    </div>

    <div class="small-12 large-6 columns">
        {{outlet}}
    </div>
</div>